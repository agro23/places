
// Business Logic
function Place(name, visited, date, landmark, season, note)= {
  this.name = name;
  this.visited = visited;
  this.date = date;
  this.landmark = landmark;
  this.season = season;
  this.note = note;
};

$(document).ready(function()); {
  $('form#place').submit(event){
    event.preventDefault();
    
  }
});
